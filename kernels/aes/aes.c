// AES-128 bare-metal kernel for Vortex SimX with a memory-mapped fault_ctrl injection hook
// Authors: Dillibabu Shanmugam and Patrick Schaumont
// Affiliation: Worcester Polytechnic Institute

#include <stdint.h>

// AES-128 S-box
static const uint8_t sbox[256] = {
    
};

static const uint8_t rcon[10] = {0x01,0x02,0x04,0x08,0x10,0x20,0x40,0x80,0x1b,0x36};

// xtime: multiply by x in GF(2^8)
static inline uint8_t xtime(uint8_t a) {
    return (a << 1) ^ ((a >> 7) * 0x1b);
}

static void key_expansion(const uint8_t *key, uint8_t *rk) {
    for (int i = 0; i < 16; i++) rk[i] = key[i];
    for (int i = 4; i < 44; i++) {
        uint8_t t[4];
        t[0] = rk[(i-1)*4 + 0];
        t[1] = rk[(i-1)*4 + 1];
        t[2] = rk[(i-1)*4 + 2];
        t[3] = rk[(i-1)*4 + 3];
        if (i % 4 == 0) {
            uint8_t tmp = t[0];
            t[0] = sbox[t[1]] ^ rcon[i/4 - 1];
            t[1] = sbox[t[2]];
            t[2] = sbox[t[3]];
            t[3] = sbox[tmp];
        }
        for (int j = 0; j < 4; j++)
            rk[i*4 + j] = rk[(i-4)*4 + j] ^ t[j];
    }
}

static void add_round_key(uint8_t *state, const uint8_t *rk) {
    for (int i = 0; i < 16; i++) state[i] ^= rk[i];
}

static void sub_bytes(uint8_t *state) {
    for (int i = 0; i < 16; i++) state[i] = sbox[state[i]];
}

static void shift_rows(uint8_t *state) {
    uint8_t tmp;
    // Row 1: shift left 1
    tmp = state[1];
    state[1] = state[5]; state[5] = state[9]; state[9] = state[13]; state[13] = tmp;
    // Row 2: shift left 2
    tmp = state[2]; state[2] = state[10]; state[10] = tmp;
    tmp = state[6]; state[6] = state[14]; state[14] = tmp;
    // Row 3: shift left 3 (= shift right 1)
    tmp = state[15];
    state[15] = state[11]; state[11] = state[7]; state[7] = state[3]; state[3] = tmp;
}

static void mix_columns(uint8_t *state) {
    for (int c = 0; c < 4; c++) {
        uint8_t s0 = state[c*4+0], s1 = state[c*4+1], s2 = state[c*4+2], s3 = state[c*4+3];
        uint8_t t = s0 ^ s1 ^ s2 ^ s3;
        state[c*4+0] ^= t ^ xtime(s0 ^ s1);
        state[c*4+1] ^= t ^ xtime(s1 ^ s2);
        state[c*4+2] ^= t ^ xtime(s2 ^ s3);
        state[c*4+3] ^= t ^ xtime(s3 ^ s0);
    }
}

// Memory-mapped fault injection control region (SimX intercepts reads/writes here)
volatile uint32_t fault_ctrl[8] __attribute__((section(".fault_ctrl"))) = {0};
// fault_ctrl layout: [0] round (0 = none, 1-10 = before this round), [1] byte (0-15), [2] XOR mask, [3] thread id (0xFFFFFFFF = all)

void aes128_encrypt(const uint8_t *plaintext, const uint8_t *key, uint8_t *ciphertext) {
    uint8_t rk[176];
    uint8_t state[16];

    key_expansion(key, rk);

    for (int i = 0; i < 16; i++) state[i] = plaintext[i];
    add_round_key(state, rk);

    for (int round = 1; round < 10; round++) {
        // Fault injection point: at INPUT of this round
        if (fault_ctrl[0] == (uint32_t)round) {
            uint32_t fb = fault_ctrl[1];
            uint32_t fv = fault_ctrl[2];
            if (fb < 16) state[fb] ^= (uint8_t)fv;
        }
        sub_bytes(state);
        shift_rows(state);
        mix_columns(state);
        add_round_key(state, rk + round*16);
    }
    // Round 10: no MixColumns
    if (fault_ctrl[0] == 10) {
        uint32_t fb = fault_ctrl[1];
        uint32_t fv = fault_ctrl[2];
        if (fb < 16) state[fb] ^= (uint8_t)fv;
    }
    sub_bytes(state);
    shift_rows(state);
    add_round_key(state, rk + 160);

    for (int i = 0; i < 16; i++) ciphertext[i] = state[i];
}

// Result memory region - placed at fixed address so SimX can read it back
volatile uint8_t result_buf[64] __attribute__((section(".result_buf"))) = {0};
// result_buf layout: [0..15] plaintext, [16..31] key, [32..47] ciphertext, [48] done flag, [49] thread id

int main(void) {
    // NIST AES-128 test vector - used as default plaintext/key
    uint8_t pt[16] = {0x32,0x43,0xf6,0xa8,0x88,0x5a,0x30,0x8d,
                       0x31,0x31,0x98,0xa2,0xe0,0x37,0x07,0x34};
    uint8_t key[16] = {0x2b,0x7e,0x15,0x16,0x28,0xae,0xd2,0xa6,
                        0xab,0xf7,0x15,0x88,0x09,0xcf,0x4f,0x3c};
    uint8_t ct[16];

    // Read overrides from result_buf if SimX wrote them before launch
    for (int i = 0; i < 16; i++) {
        if (result_buf[i] != 0) pt[i] = result_buf[i];
    }
    for (int i = 0; i < 16; i++) {
        if (result_buf[16+i] != 0) key[i] = result_buf[16+i];
    }

    aes128_encrypt(pt, key, ct);

    // Write outputs back
    for (int i = 0; i < 16; i++) {
        result_buf[i] = pt[i];
        result_buf[16+i] = key[i];
        result_buf[32+i] = ct[i];
    }
    result_buf[48] = 1;  // done flag

    return 0;
}
